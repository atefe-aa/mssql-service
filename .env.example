# Database Configuration
DB_SERVER=localhost
DB_PORT=1433
DB_NAME=your_database_name

# Authentication Mode: "windows" or "sql"
DB_AUTH_MODE=windows

# SQL Authentication (only required if DB_AUTH_MODE=sql)
# Leave empty for Windows Authentication
DB_USER=
DB_PASSWORD=

# Server Configuration
SERVER_PORT=8080